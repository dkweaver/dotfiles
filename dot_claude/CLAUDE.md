# Claude Global Instructions
## Git Workflow - CRITICAL

**NEVER PUSH DIRECTLY TO MAIN BRANCH**

**NEVER RUN: `git push origin main`**

This is non-negotiable. If you need to push changes, ask the user first.
